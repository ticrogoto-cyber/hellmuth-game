{
  "name": "hellmuth",
  "version": "0.0.1",
  "description": "HELLMUTH — Browser-RTS: DIE HELLMUTH gegen DIE MODERAT. Phaser 3 + TypeScript + Vite.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:audio": "node --experimental-strip-types test/voice_limiter.test.ts && node --experimental-strip-types test/audio_mix.test.ts && node --experimental-strip-types test/audio_content.test.ts && node --experimental-strip-types test/manifest_resolve.test.ts && node --experimental-strip-types test/audio_robust.test.ts && node --experimental-strip-types test/audio_destillat.test.ts",
    "gate:assets": "python3 tools/asset_resolve_gate.py",
    "gate:grep": "bash tools/grep_gates.sh",
    "gate:coupling": "bash tools/coupling_gate.sh",
    "gate:baseline": "python3 tools/baseline_gate.py",
    "gate:werkzeuge": "python3 tools/werkzeuge_check.py",
    "lint:auftrag": "python3 tools/lint_auftragsbrief.py",
    "report:knip": "knip --no-progress",
    "report:madge": "madge --circular --extensions ts src/"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "knip": "^6.17.1",
    "madge": "^8.0.0",
    "odiff-bin": "^4.3.8",
    "playwright": "^1.60.0",
    "tsx": "^4.22.4",
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  }
}
